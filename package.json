{
  "name": "pause-for-paws",
  "version": "1.0.0",
  "private": true,
  "description": "Historical wildlife corridor warning prototype with web deployment and Android wrapper support.",
  "scripts": {
    "start": "python -m http.server 5500",
    "serve": "python -m http.server 5500",
    "dev": "python -m http.server 5500",
    "build": "echo 'Static site build complete. No bundler is required for this prototype.'",
    "android:sync": "node scripts/sync-web.js && npx cap sync android",
    "android:open": "npx cap open android",
    "android:run": "npx cap run android",
    "android:build": "npx cap build android",
    "ios:sync": "node scripts/sync-web.js && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "ios:run": "npx cap run ios",
    "ios:build": "npx cap build ios"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1",
    "typescript": "^5.6.3"
  }
}
